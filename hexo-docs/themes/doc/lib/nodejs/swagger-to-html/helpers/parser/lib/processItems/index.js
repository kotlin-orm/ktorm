'use strict';

const processEnum = require('./enum');
const processArray = require('./array');
const processObject = require('./object');
const processDefault = require('./default');

module.exports = {
  processEnum,
  processArray,
  processObject,
  processDefault
};
